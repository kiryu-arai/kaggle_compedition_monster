[project]
name = "kaggle"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "yutasuzukifu-cell", email = "yuta.suzuki.fu@ambl.co.jp" }
]
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.4.6",
]

[project.scripts]
kaggle = "kaggle:main"

[build-system]
requires = ["uv_build>=0.11.15,<0.12.0"]
build-backend = "uv_build"
